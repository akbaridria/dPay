export interface Menus {
  name: string,
  link: string,
  icon: string
}

export interface Benefits {
  image: string,
  title: string,
  subtitle: string
}

export interface StreamingFlow {
  number: string,
  title: string,
  description: string
}